cmake_minimum_required(VERSION 2.8.12)
project(projeto)

# Definir a versão do C++
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Adicionar o diretório da dlib
add_subdirectory(./dlib dlib_build)

# Adicionar um alvo para a biblioteca compartilhada
add_library(main SHARED main.cpp)

# Linkar a biblioteca com a dlib
target_link_libraries(main dlib::dlib)

# Adicionar os executáveis (se ainda precisar deles)
#add_executable(extract_face_metrics extractMetrics.cpp)
#target_link_libraries(extract_face_metrics dlib::dlib)

#add_executable(compare_face_metrics faceCompar.cpp)
#target_link_libraries(compare_face_metrics dlib::dlib)